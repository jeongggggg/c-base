#include <stdio.h>

// 비트 연산자
int main() {
    int i=1, j=9;
    printf("%d & %d= %d\n", i, j, i & j); 
    printf("%d | %d= %d\n", i, j, i | j); 
    printf("%d ^ %d= %d\n", i, j, i ^ j); 
    return 0;
}